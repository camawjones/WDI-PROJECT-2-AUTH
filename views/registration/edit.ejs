<div class="container">
<form class="col-6 form-gorup" method="POST" action="/users/<%= currentUser._id %>">
  <input type="hidden" name="_method" value="PUT" class="form-control">
  <div>
    <label for="firstName">First Name</label>
    <input class="form-control" type="text" name="firstName" id="firstName" required minlength="2" value="<%= currentUser.firstName %>">
  </div>
  <div>
    <label for="lastName">Last Name</label>
    <input class="form-control" type="text" name="lastName" id="lastName" required minlength="2" value="<%= currentUser.lastName %>">
  </div>
  <div>
    <label for="username">Username</label>
    <input class="form-control" type="text" name="username" id="username" required value="<%= currentUser.username %>">
  </div>
  <div>
    <label for="email">Email</label>
    <input class="form-control" type="email" name="email" id="email" required value="<%= currentUser.email %>">
  </div>
  <div>
    <label for="skills">Skills</label>
    <% skills.forEach(skill => { %>
      <label class="checkbox-inline">
        <% if ((`${currentUser.skills}`).includes(`${skill.id}`)) { %>
          <input class="form-control" type="checkbox" name="skills" value="<%= skill.id %>" checked><i class="<%= skill.icon %> colored"></i>
        <% } else { %>
          <input class="form-control" type="checkbox" name="skills" value="<%= skill.id %>"><i class="<%= skill.icon %> colored"></i>
        <% } %>
      </label>
    <% }); %>
  </div>

  <div>
    <label for="password">Password</label>
    <input class="form-control" type="password" name="password" id="password" required>
  </div>
  <div>
    <label for="passwordConfirmation">Password Confirmation</label>
    <input class="form-control" type="password" name="passwordConfirmation" id="passwordConfirmation" required>
  </div>

  <button style="margin-top: 20px" class="btn btn-primary noredbutton logins">Edit Profile</button>
</form>
</div>
